const NotesMapper = (data) => {
    const { notes, keyMap } = data;
  
    console.log(notes);
    console.log(keyMap);
  
    let script = "testeeeees";
  
    return script;
  };
  
  export default NotesMapper;
  